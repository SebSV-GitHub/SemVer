name: "Hello World"
description: "Greet someone"
inputs:
  who-to-greet:
    description: "Who to greet"
    required: true
    default: "World"
outputs:
  message:
    description: "Crafted message"
runs:
  using: "node20"
  main: "dist/index.js"
